# Live-Modellaufrufe: 0 = aus (Standard). Die Demo zeigt dann die aufgezeichneten
# Ergebnisse, Freigabe und Ablehnung funktionieren, "Verarbeiten" ist gesperrt.
# 1 = an: jeder Klick auf "Verarbeiten" ruft das Modell und kostet Geld.
# Eine öffentlich erreichbare Instanz bleibt auf 0, sonst zahlt der Schlüssel für jeden Besucher.
LIVE_MODELLAUFRUFE=0
# Anbieter: ollama (Standard, lokal) oder openai; jeder andere OpenAI-kompatible
# Endpunkt läuft über LLM_BASE_URL mit eigenem Schlüssel.
LLM_PROVIDER=ollama
# Basis-URL des Anbieters. Für OpenAI: https://api.openai.com/v1
LLM_BASE_URL=http://localhost:11434/v1
# Modellname. Ollama z. B. gpt-oss:20b oder qwen2.5:14b-instruct
LLM_MODEL=gpt-oss:20b
# Schlüssel eines eigenen Kontos. Nie einchecken. Bei Ollama beliebiger Wert.
LLM_API_KEY=ollama
# Kontextfenster für Ollama. Ollamas Standard von 4096 Token reicht für gpt-oss:20b
# nicht: das Modell füllt ihn bei vagen Mails mit Reasoning und liefert eine leere
# Antwort. Gilt nur für den Anbieter ollama, andere Anbieter ignorieren den Wert.
LLM_NUM_CTX=8192
# Zeitlimit eines Modellaufrufs in Sekunden. Ohne diesen Wert gilt der Standard des
# openai-SDK von 600 Sekunden; ein hängender Anbieter blockiert dann zehn Minuten
# lang die Verarbeitung und im Server die HTTP-Anfrage. Standard hier 120.
LLM_TIMEOUT=120
# Basis-URL der Mock-Systemlandschaft. Leer = im selben Prozess (ASGI), sonst z. B.
# http://systeme:8050 (Compose) oder http://localhost:8050 (Sidecar).
# SYSTEME_BASE_URL=
# Port des Servers im Container (Render und Fly setzen ihn selbst). run.bat/run.sh nutzen fest 8040.
PORT=8040
